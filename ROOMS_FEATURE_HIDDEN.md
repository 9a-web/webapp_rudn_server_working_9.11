# Функция комнат скрыта в разделе "Список дел"

## Дата: 16 ноября 2024

## Изменения

Функция **Командных задач (комнат)** была **СКРЫТА** (не удалена!) в разделе "Список дел".

### Что было сделано:

1. **Добавлена константа-переключатель** в `/app/frontend/src/components/TasksSection.jsx`:
   ```javascript
   // 🔧 FEATURE FLAG: Показать/скрыть функцию комнат
   // Измените на true, чтобы снова показать комнаты
   const SHOW_ROOMS_FEATURE = false;
   ```

2. **Скрыты UI элементы комнат**:
   - Заголовок секции "Комнаты" с иконкой Users
   - Горизонтальный скролл с карточками комнат (RoomCard)
   - Кнопка "Создать комнату"
   - Модальные окна: CreateRoomModal и RoomDetailModal

3. **Оптимизирована загрузка данных**:
   - useEffect для загрузки комнат не выполняется когда функция скрыта
   - Состояния модальных окон комнат игнорируются при отслеживании

### Что НЕ было удалено:

- ✅ Все импорты компонентов комнат (RoomCard, CreateRoomModal, RoomDetailModal, roomsAPI)
- ✅ Все состояния комнат (rooms, isCreateRoomModalOpen, selectedRoom, isRoomDetailModalOpen)
- ✅ Все функции обработки комнат (handleCreateRoom, handleRoomClick, handleRoomDeleted, loadRooms)
- ✅ Весь код backend для комнат (/app/backend/models.py, /app/backend/server.py)
- ✅ Все API endpoints комнат (POST /api/rooms, GET /api/rooms/{telegram_id}, etc.)

### Как вернуть функцию комнат:

Чтобы снова показать функцию комнат, нужно изменить **ОДНУ СТРОКУ** в файле:
`/app/frontend/src/components/TasksSection.jsx`

```javascript
// Было:
const SHOW_ROOMS_FEATURE = false;

// Изменить на:
const SHOW_ROOMS_FEATURE = true;
```

После изменения перезапустить frontend:
```bash
sudo supervisorctl restart frontend
```

## Результаты тестирования

### ✅ Успешно:
- Frontend компилируется без ошибок
- Раздел "Список дел" открывается корректно
- Секция "Комнаты" полностью отсутствует в интерфейсе
- Все остальные функции работают нормально
- Backend API комнат остались доступны (но не используются)

### Скриншот раздела "Список дел" после изменений:
- Отображается заголовок "Список дел" с иконкой
- Селектор дней недели работает
- Карточка задач "Сегодня" отображается
- Кнопка "Добавить задачу" работает
- **Секция "Комнаты" отсутствует** ✅

## Технические детали

### Изменённые файлы:
- `/app/frontend/src/components/TasksSection.jsx` (7 мест использования SHOW_ROOMS_FEATURE)

### Затронутые компоненты (скрыты, не удалены):
- `RoomCard` - карточка комнаты
- `CreateRoomModal` - модальное окно создания комнаты
- `RoomDetailModal` - модальное окно деталей комнаты
- `roomsAPI` - API для работы с комнатами

### Backend (не изменён):
- Все endpoints комнат продолжают работать
- Модели Room, RoomParticipant сохранены
- База данных комнат не затронута

## Заключение

Функция комнат **полностью скрыта** из раздела "Список дел", но **не удалена** из кода.
Это позволяет быстро вернуть функционал в будущем, изменив одну константу.
