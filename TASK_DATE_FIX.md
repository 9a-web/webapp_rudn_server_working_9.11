# Настройка дедлайна по умолчанию при создании задачи

## История изменений

### Версия 2 (Текущая)
**Требование:** По умолчанию при создании задачи значение дедлайна должно быть "нет" (пустое).

**Решение:** Убрана автоматическая установка дедлайна. Теперь поле дедлайна пустое по умолчанию, и пользователь сам решает, устанавливать дедлайн или нет.

#### Изменения в AddTaskModal.jsx:
```jsx
export const AddTaskModal = ({ 
  isOpen, 
  onClose, 
  onAddTask, 
  hapticFeedback,
  scheduleSubjects = [] // Список предметов из расписания
}) => {
  const [deadline, setDeadline] = useState(''); // По умолчанию пустое значение (нет дедлайна)
  // ... остальной код
```

#### Результат v2:
✅ При открытии модального окна "Новая задача" поле "Дедлайн" пустое  
✅ Пользователь сам выбирает, нужен ли дедлайн для задачи  
✅ Если дедлайн не указан, задача создается без срока выполнения  

---

### Версия 1 (Устаревшая)
**Первоначальное требование:** При выборе даты в селекторе дней недели, новая задача должна создаваться на эту дату.

**Решение v1:** Автоматическая установка дедлайна на выбранную дату при открытии модального окна.

**Почему изменили:** Пользователь запросил изменение поведения - дедлайн должен быть пустым по умолчанию.

---

## Текущее поведение

### При создании задачи:
1. Пользователь нажимает кнопку "Добавить задачу" (плюс)
2. Открывается модальное окно "Новая задача"
3. Все поля пустые, кроме:
   - **Приоритет:** "Средний" (по умолчанию)
   - **Дедлайн:** пустое поле (пользователь может установить при желании)
4. Пользователь заполняет поля по своему усмотрению
5. При сохранении задача создается с указанными параметрами

### Поле "Дедлайн":
- **По умолчанию:** Пустое (нет дедлайна)
- **Тип:** `datetime-local` (дата и время)
- **Опционально:** Пользователь может не указывать дедлайн
- **Если не указан:** Задача попадет в группу "Без срока"

## Измененные файлы
- `/app/frontend/src/components/AddTaskModal.jsx` (строки 6-26)
- `/app/frontend/src/components/TasksSection.jsx` (строка 824-830)

## Статус
🟢 Изменение завершено. Frontend скомпилирован успешно.
