# Исправление бага: Дата создания задачи в разделе "Список дел"

## Проблема
При выборе даты в селекторе дней недели, новая задача создавалась на сегодняшний день вместо выбранной даты.

## Причина
В компонент `AddTaskModal` не передавалась выбранная дата (`tasksSelectedDate`) из `TasksSection`, поэтому поле дедлайна не заполнялось автоматически выбранной датой.

## Решение

### 1. TasksSection.jsx
Добавлена передача выбранной даты в модальное окно:

```jsx
<AddTaskModal
  isOpen={isAddModalOpen}
  onClose={() => setIsAddModalOpen(false)}
  onAddTask={handleAddTask}
  hapticFeedback={hapticFeedback}
  scheduleSubjects={scheduleSubjects}
  selectedDate={tasksSelectedDate}  // ← Новый проп
/>
```

### 2. AddTaskModal.jsx

#### a) Добавлен новый проп
```jsx
export const AddTaskModal = ({ 
  isOpen, 
  onClose, 
  onAddTask, 
  hapticFeedback,
  scheduleSubjects = [],
  selectedDate = new Date()  // ← Новый проп с значением по умолчанию
}) => {
```

#### b) Добавлен useEffect для автоматической установки дедлайна
```jsx
// Устанавливаем дедлайн на выбранную дату при открытии модального окна
useEffect(() => {
  if (isOpen && selectedDate) {
    // Форматируем дату для input type="datetime-local" (YYYY-MM-DDTHH:MM)
    const date = new Date(selectedDate);
    // Устанавливаем время на конец дня (23:59)
    date.setHours(23, 59, 0, 0);
    const formattedDate = date.toISOString().slice(0, 16);
    setDeadline(formattedDate);
  }
}, [isOpen, selectedDate]);
```

## Результат
✅ Теперь при выборе даты в селекторе дней недели и нажатии кнопки "Добавить задачу", поле дедлайна автоматически заполняется выбранной датой (время устанавливается на 23:59).

✅ Пользователь может изменить дату/время при необходимости.

✅ Frontend скомпилирован успешно без ошибок.

## Измененные файлы
- `/app/frontend/src/components/TasksSection.jsx` (строка 829)
- `/app/frontend/src/components/AddTaskModal.jsx` (строки 12, 24-33)

## Статус
🟢 Исправление завершено и готово к тестированию
