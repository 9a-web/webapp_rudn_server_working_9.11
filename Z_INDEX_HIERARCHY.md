# Z-Index иерархия компонентов

## 📊 Структура слоев приложения

Правильная иерархия z-index критична для корректного отображения модальных окон и всплывающих элементов.

---

## 🎯 Текущая иерархия (от нижнего к верхнему)

```
┌─────────────────────────────────────────────┐
│  z-10000  - AddTaskModal content           │  ← Самый верхний слой
├─────────────────────────────────────────────┤
│  z-9999   - AddTaskModal backdrop          │
├─────────────────────────────────────────────┤
│  z-160    - ShareScheduleModal content     │
├─────────────────────────────────────────────┤
│  z-150    - ShareScheduleModal backdrop    │
├─────────────────────────────────────────────┤
│  z-101    - ProfileModal content           │
├─────────────────────────────────────────────┤
│  z-100    - ProfileModal backdrop          │
├─────────────────────────────────────────────┤
│  z-50     - BottomNavigation               │
├─────────────────────────────────────────────┤
│  z-10     - Icons inside navigation        │
├─────────────────────────────────────────────┤
│  z-1      - Regular content                │
├─────────────────────────────────────────────┤
│  z-0      - Base layer (default)           │
└─────────────────────────────────────────────┘
```

---

## 📝 Компоненты и их z-index

### 1. **AddTaskModal** (Модальное окно создания задачи)
- **Backdrop**: `z-[9999]`
- **Content**: `z-[10000]`
- **Файл**: `/app/frontend/src/components/AddTaskModal.jsx`
- **Причина**: Должно быть поверх всех элементов, включая Bottom Navigation

### 2. **ShareScheduleModal** (Модальное окно поделиться)
- **Backdrop**: `z-[150]`
- **Content**: `z-[160]`
- **Файл**: `/app/frontend/src/components/ShareScheduleModal.jsx`
- **Примечание**: Может потребоваться увеличение если будет перекрываться

### 3. **ProfileModal** (Профиль пользователя)
- **Backdrop**: `z-[100]`
- **Content**: `z-[101]`
- **Файл**: `/app/frontend/src/components/ProfileModal.jsx`
- **Примечание**: Может потребоваться увеличение если будет перекрываться

### 4. **BottomNavigation** (Нижняя навигация)
- **Container**: `z-50`
- **Icons**: `z-10` (relative)
- **Файл**: `/app/frontend/src/components/BottomNavigation.jsx`
- **Причина**: Должна быть поверх контента, но под модальными окнами

---

## ⚠️ Правила работы с z-index

### 1. Модальные окна
- Всегда используйте z-index > 1000 для модальных окон
- Backdrop должен быть на 1 меньше чем content
- Новые модальные окна должны использовать z-index > 5000

### 2. Навигация
- Bottom Navigation: z-50
- Top Navigation/Header: z-40 (если есть)
- Sidebar: z-30 (если есть)

### 3. Контент
- Обычный контент: z-1 или z-0 (default)
- Sticky элементы: z-10 - z-20
- Floating elements: z-20 - z-30

### 4. Overlay элементы
- Tooltips: z-100 - z-200
- Dropdowns: z-100 - z-200
- Context menus: z-100 - z-200

---

## 🔧 Проблемы и решения

### Проблема 1: Модальное окно перекрывается навигацией
**Симптомы**: Bottom Navigation виден поверх модального окна

**Решение**:
```jsx
// ❌ Неправильно
className="fixed inset-0 z-50"

// ✅ Правильно
className="fixed inset-0 z-[9999]"
```

### Проблема 2: Несколько модальных окон друг над другом
**Решение**: Используйте разные диапазоны:
- Первое модальное: z-[1000] - z-[1001]
- Второе модальное: z-[2000] - z-[2001]
- Третье модальное: z-[3000] - z-[3001]

### Проблема 3: Dropdown перекрывается модальным окном
**Решение**: Dropdown внутри модального окна должен иметь относительный z-index
```jsx
// Внутри модального окна
<div className="relative z-10">
  <Dropdown />
</div>
```

---

## 📋 Чеклист при добавлении нового модального окна

- [ ] Backdrop имеет z-index > 5000
- [ ] Content имеет z-index = backdrop + 1
- [ ] Проверено на мобильных устройствах
- [ ] Не перекрывается Bottom Navigation
- [ ] Backdrop блокирует клики на контент под ним
- [ ] ESC закрывает модальное окно
- [ ] Клик вне модального окна закрывает его

---

## 🐛 История исправлений

### v2 (Текущая версия)
- **Дата**: 2025-11-07
- **Проблема**: AddTaskModal перекрывается Bottom Navigation
- **Решение**: Увеличен z-index до 9999/10000
- **Файлы**: AddTaskModal.jsx

### v1 (Предыдущая версия)
- **Дата**: Ранее
- **Проблема**: Первая попытка исправления z-index
- **Решение**: z-100/z-101 (было недостаточно)
- **Файлы**: AddTaskModal.jsx

---

## 🔮 Рекомендации для будущего

1. **Централизованные константы**
   ```javascript
   // constants/zIndex.js
   export const Z_INDEX = {
     BASE: 0,
     CONTENT: 1,
     STICKY: 10,
     NAVIGATION: 50,
     DROPDOWN: 100,
     MODAL_BACKDROP: 9999,
     MODAL_CONTENT: 10000,
   };
   ```

2. **Tailwind config**
   ```javascript
   // tailwind.config.js
   module.exports = {
     theme: {
       extend: {
         zIndex: {
           'modal-backdrop': '9999',
           'modal-content': '10000',
           'navigation': '50',
         }
       }
     }
   }
   ```

3. **CSS переменные**
   ```css
   :root {
     --z-modal-backdrop: 9999;
     --z-modal-content: 10000;
     --z-navigation: 50;
   }
   ```

---

## ✅ Проверка корректности

Используйте этот чеклист для проверки:

1. Откройте модальное окно AddTask
2. Убедитесь что Bottom Navigation не видна
3. Попробуйте кликнуть на Bottom Navigation (должно быть заблокировано)
4. Закройте модальное окно
5. Bottom Navigation должна снова быть активной

---

## 📞 Контакты для вопросов

При возникновении проблем с z-index:
1. Проверьте эту документацию
2. Убедитесь что новый элемент использует правильный диапазон
3. Проверьте stacking context родительских элементов
4. Используйте DevTools для отладки
