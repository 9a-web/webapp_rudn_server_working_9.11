# 🧪 Тестирование Full Screen режима Telegram Web App

## Как проверить, что Full Screen работает

### 1. Откройте приложение в Telegram

Откройте бота и запустите Web App

### 2. Проверьте консоль браузера (для Telegram Desktop)

Откройте DevTools в Telegram Desktop:
- **Windows/Linux:** `Ctrl + Shift + I`
- **macOS:** `Cmd + Option + I`

### 3. Ищите следующие логи в консоли:

```
🟢 [index.html] Начало инициализации Telegram WebApp
🟢 [index.html] Telegram WebApp API загружен
📊 [index.html] Состояние до ready(): { isExpanded: false, viewportHeight: X }
✅ [index.html] tg.ready() вызван
🔄 [index.html] Попытка expand #1
📏 [index.html] isExpanded: true, viewportHeight: XXX
🎉 [index.html] УСПЕХ! WebApp развернут на полный экран

🔵 Инициализация Telegram WebApp...
📊 Начальное состояние: { isExpanded: true, viewportHeight: XXX }
🔄 Попытка expand()... isExpanded: true
✅ Вертикальные свайпы отключены
✅ Подтверждение закрытия включено
✅ Цвета темы установлены
📐 Viewport переменные обновлены: { height: XXX, stableHeight: XXX }
🚀 Telegram WebApp инициализирован
```

### 4. Проверьте визуально

**✅ Что должно быть:**
- Приложение занимает весь экран от верха до низа
- Нет белых полос сверху или снизу
- Header Telegram не виден (скрыт)
- Приложение выглядит как нативное

**❌ Что НЕ должно быть:**
- Белая полоса сверху с header Telegram
- Белая полоса снизу
- Приложение в "окне" внутри Telegram

### 5. Проверьте значение `isExpanded`

В консоли выполните:
```javascript
window.Telegram.WebApp.isExpanded
// Должно вернуть: true
```

Проверьте высоту viewport:
```javascript
window.Telegram.WebApp.viewportHeight
// Должно вернуть: число > 500 (зависит от устройства)
```

### 6. Тест на мобильном устройстве

**iOS:**
1. Откройте бота в Telegram
2. Приложение должно занять весь экран без header Telegram
3. При скролле не должен появляться header

**Android:**
1. Откройте бота в Telegram
2. Приложение должно занять весь экран
3. Системная панель уведомлений может быть видна (это нормально)

## Проблемы и решения

### Проблема: `isExpanded` остается `false`

**Причины:**
1. Старая версия Telegram (< 6.0)
2. Web версия Telegram (не все методы поддерживаются)
3. Проблема с инициализацией

**Решение:**
1. Обновите Telegram до последней версии
2. Используйте нативные приложения (не Web версию)
3. Проверьте консоль на ошибки

### Проблема: Приложение не на весь экран

**Проверьте:**
1. Версию Telegram: `window.Telegram.WebApp.version`
2. Платформу: `window.Telegram.WebApp.platform`
3. Логи в консоли

**Возможные причины:**
- Telegram версии < 6.0 не поддерживает `expand()`
- Web версия Telegram имеет ограничения
- Некоторые платформы не поддерживают полный Full Screen

### Проблема: Белая полоса сверху

**Решение:**
Проверьте, что в консоли есть лог:
```
✅ Цвета темы установлены
```

Если нет, проверьте версию Telegram.

## Совместимость

| Платформа | Минимальная версия | Full Screen |
|-----------|-------------------|-------------|
| iOS | 9.0+ | ✅ Да |
| Android | 9.0+ | ✅ Да |
| Desktop | 4.0+ | ✅ Да |
| Web (k/z) | 7.0+ | ⚠️ Частично |

## Дополнительная информация

### Проверка версии Telegram

В консоли:
```javascript
console.log({
  version: window.Telegram.WebApp.version,
  platform: window.Telegram.WebApp.platform,
  isExpanded: window.Telegram.WebApp.isExpanded,
  viewportHeight: window.Telegram.WebApp.viewportHeight
});
```

### CSS переменные

Проверьте, что установлены:
```javascript
console.log({
  tgViewportHeight: getComputedStyle(document.documentElement).getPropertyValue('--tg-viewport-height'),
  tgStableHeight: getComputedStyle(document.documentElement).getPropertyValue('--tg-viewport-stable-height')
});
```

## Обратная связь

Если Full Screen не работает, пожалуйста, предоставьте:

1. **Версию Telegram:** (из консоли)
2. **Платформу:** (iOS/Android/Desktop/Web)
3. **Значение `isExpanded`:** (true/false)
4. **Логи консоли:** (скриншот или текст)
5. **Скриншот приложения:** (покажите, как выглядит)

Это поможет диагностировать проблему!
